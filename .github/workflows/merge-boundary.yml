name: Behavioral merge boundary
on:
  pull_request:
    branches: [merge-boundary-base]
permissions:
  contents: read
concurrency:
  group: merge-boundary-${{ github.event.pull_request.number }}
  cancel-in-progress: true
jobs:
  gate:
    name: Behavioral merge gate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      checks: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: false
      # Reviewed upstream implementation; candidate PRs do not supply Action code.
      - uses: maida-ai/maida-assert@92980030c9a8d30106db11804826b7fd61b740c8
        env:
          MAIDA_DATA_DIR: ${{ runner.temp }}/merge-boundary-runs
        with:
          agent-script: agent.py
          baseline: .maida/baselines/sales.json
          policy: ${{ github.head_ref == 'merge-boundary-inconclusive' && '.maida/inconclusive.yaml' || '.maida/policy.yaml' }}
          mode: ${{ github.head_ref == 'merge-boundary-report-only' && 'report-only' || 'blocking' }}
          configuration-acceptance: ${{ vars.MAIDA_CONFIGURATION_ACCEPTANCE }}
          post-comment: 'false'
